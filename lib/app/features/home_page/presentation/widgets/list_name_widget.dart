import 'package:flutter/material.dart';

class ListNameWidget extends StatelessWidget {
  const ListNameWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
